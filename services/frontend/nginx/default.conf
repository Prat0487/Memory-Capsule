# Remove the upstream block entirely
# upstream memory-service {
#   server memory-service:3000;
# }

# Add Docker's DNS resolver
resolver 127.0.0.11 valid=30s;

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # For SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API proxy with deferred DNS resolution
    location /api/ {
        # This delays DNS resolution until request time
        set $backend_server memory-service:3000;
        proxy_pass http://$backend_server;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # Static files caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
    }
}
